name: Public API

on:
  pull_request:
  push:

jobs:
  public-api:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate public API surface
        run: Tools/public-api/validate-public-surface.sh

      - name: Run tests
        run: swift test
